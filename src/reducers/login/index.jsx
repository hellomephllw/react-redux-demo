/**
 * Created by liliwen on 2017/1/29.
 */
'use strict';

const loginReducer = (state = {}, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default loginReducer;